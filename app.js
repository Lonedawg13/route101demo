const CONFIG={voiceUrl:"PASTE_VOICE_AGENT_URL_HERE",chatUrl:"PASTE_CHAT_DEMO_URL_HERE",embedVoice:false,embedChat:false};
function initMenu(){const b=document.querySelector('.mobile-toggle'),n=document.querySelector('.links');if(b&&n)b.addEventListener('click',()=>n.classList.toggle('open'))}
function initAgent(){const root=document.querySelector('[data-agent]');if(!root)return;const type=root.dataset.agent,url=type==='voice'?CONFIG.voiceUrl:CONFIG.chatUrl,embed=type==='voice'?CONFIG.embedVoice:CONFIG.embedChat,configured=url&&!url.startsWith('PASTE_'),label=type==='voice'?'voice agent':'chat demo';if(!configured)return;if(embed){root.innerHTML=`<iframe src="${url}" title="Route101 ${label}" ${type==='voice'?'allow="microphone; autoplay"':''}></iframe>`}else{root.querySelector('h2').textContent='Your agent is ready';root.querySelector('.agent-message').textContent=`Launch the ${label} in a secure new tab.`;const pill=root.querySelector('.code-pill');pill.outerHTML=`<a class="btn btn-primary" href="${url}" target="_blank" rel="noopener noreferrer">Launch ${label} →</a>`}}
document.addEventListener('DOMContentLoaded',()=>{initMenu();initAgent()});